{
  "name": "cv-taherjamali",
  "private": true,
  "workspaces": [
    "cv-ui",
    "cv-chat-service"
  ],
  "scripts": {
    "dev": "concurrently -n UI,CHAT -c blue,magenta \"npm run dev --workspace=cv-ui\" \"npm run dev --workspace=cv-chat-service\"",
    "build": "npm run build --workspace=cv-ui",
    "lint": "npm run lint --workspace=cv-ui"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
